{
  "name": "spotify-song-ranker",
  "version": "0.1.0",
  "private": true,
  "description": "A Spotify-powered, co-located song battle party game.",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "start": "vercel dev --listen 3000",
    "db:migrate": "node scripts/migrate.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "vercel": "^46.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
