# Create an app at https://developer.spotify.com/dashboard.
# Add: http://127.0.0.1:3000/api/auth/callback
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
APP_URL=http://127.0.0.1:3000

# Neon pooled Postgres connection string.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Generate three different long random values. Keep all of them private.
SESSION_SECRET=replace_with_at_least_32_random_characters
TOKEN_ENCRYPTION_KEY=replace_with_at_least_32_random_characters
GROUP_INVITE_CODE=replace_with_a_private_invite_code
